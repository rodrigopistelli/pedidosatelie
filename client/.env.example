# Copie para .env (nunca commite o .env real)
# Local: deixe vazio (usa o proxy do Vite). Produção (Vercel): URL da API no Render, sem barra no final.
# VITE_API_URL=https://pedidosatelie-api.onrender.com
